# Sample Vault

This folder contains public, generic demo notes for VaultMind.

Use it as a starting point for local demos:

```bash
cp -R examples/vault data/vault
```

It is intentionally safe for GitHub:

- no personal notes
- no machine-specific paths
- no local vault exports
- only small sample markdown files
